# FileUpload Type
scalar FileUpload

type Query{
    test: String!
}

type Mutation{
    imgUpload(file: FileUpload): Boolean!

    register(
        id: String!
        pw: String!
        username: String!
    ): Boolean!
}
