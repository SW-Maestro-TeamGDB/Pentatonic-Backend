extend type Query {
    likeStatus(
        bandId: ObjectID!
    ): Boolean
}

input LikeInput {
    band: JoinBandArgsInput!
}

extend type BandLink {
    likeCount: Int!
    likeStatus: Boolean
}

extend type Band {
    likeCount: Int!
    likeStatus: Boolean
}

extend type FreeBand {
    likeCount: Int!
    likeStatus: Boolean
}

extend type Mutation {
    like(
        input: LikeInput!
    ): Boolean
}