extend type Query {
    likeStatus(
        bandId: ObjectID!
    ): Boolean
}

input LikeInput {
    band: JoinBandArgsInput!
}

extend type Mutation {
    like(
        bandId: LikeInput!
    ): Boolean
}