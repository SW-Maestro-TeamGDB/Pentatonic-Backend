extend type Mutation {
    payment(input: PaymentInput!): Boolean
}

input PaymentInput {
    cardNumber: String!
    expiry: String!
    birth: String!
    buyerName: String!
    buyerEmail: String!
    password2Digit: String!
}
