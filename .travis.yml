services:
  - mongodb
  - redis

language: node_js

node_js:
  - stable

branches:
  only:
    - master
    - develop

install:
  - apt-get update -y
  - apt-get install ffmpeg -y
  - yarn install

script:
  - yarn coverage

after_success:
  - yarn coveralls
  - yarn deploy
