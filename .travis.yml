services:
  - mongodb
  - redis

language: node_js

node_js:
  - stable

branches:
  only:
    - master
    - develop

install:
  - yarn install

script:
  - yarn coverage

after_success:
  - yarn coveralls
